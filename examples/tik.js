function solveTicTacToe(line1, line2, line3) {
  let grid = [line1[0].split(""), line2[0].split(""), line3[0].split("")];
  console.log(grid);
  let isWin = false;
  for (let i in grid) {
    if (grid[i]) {
    }
  }
}

let result = solveTicTacToe(["XOX"], ["XXX"], ["OXO"]);
console.log(result);
